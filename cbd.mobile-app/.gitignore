# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-web
*.local

# .env.web коммитим намеренно: это ПУБЛИЧНЫЙ firebase web-config (уезжает в
# браузер). Секрет — только FIREBASE_SERVICE_ACCOUNT на бэке.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Android signing keystore (do NOT commit)
*.jks
*.keystore
